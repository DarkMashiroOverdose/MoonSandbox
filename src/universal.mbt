/// ------------------------
/// 实用函数
/// ------------------------
fn min_double(a: Double, b: Double) -> Double {
  if a < b { a } else { b }
}

fn max_double(a: Double, b: Double) -> Double {
  if a > b { a } else { b }
}